# Picovoice Access Key (REQUIRED)
# Get your free access key from: https://console.picovoice.ai/
# Account → Access Keys → Create New Key
PORCUPINE_ACCESS_KEY=your_access_key_here

# Wake Words (default: albert)
# Comma-separated list of wake words to detect
# For custom wake words, must match .ppn filename in ./models/
KEYWORDS=albert

# Detection Sensitivity (default: 0.5)
# Range: 0.0 (less sensitive) to 1.0 (more sensitive)
# Higher = more detections but more false positives
# Lower = fewer false positives but might miss wake words
SENSITIVITY=0.5

# Server Configuration
HOST=0.0.0.0
PORT=10400

# Custom Model Directory (default: /app/models)
CUSTOM_MODEL_DIR=/app/models
